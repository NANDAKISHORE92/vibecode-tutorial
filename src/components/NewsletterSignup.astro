---
---

<section class="bg-violent-red text-white py-16 px-6">
  <div class="max-w-2xl mx-auto text-center">
    <h2 class="font-heading text-4xl font-bold mb-4">
      Join My Weekly Newsletter
    </h2>
    <p class="mb-8 text-lg opacity-90">
      Get updates on my latest work, thoughts, and projects delivered to your inbox every week.
    </p>
    <form class="flex flex-col sm:flex-row gap-4 max-w-md mx-auto">
      <input
        type="email"
        placeholder="Enter your email"
        class="flex-1 px-4 py-3 text-black rounded-none focus:outline-none focus:ring-2 focus:ring-white"
        required
      />
      <button
        type="submit"
        class="px-8 py-3 bg-black text-white font-heading font-bold hover:bg-gray-800 transition-colors whitespace-nowrap"
      >
        Subscribe
      </button>
    </form>
    <p class="mt-4 text-sm opacity-75">
      [Placeholder form - will be replaced with real newsletter integration]
    </p>
  </div>
</section>

